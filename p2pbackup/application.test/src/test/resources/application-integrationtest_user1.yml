at:
  lucny:
    p2p-backup:
      user: user1
      network:
        port: 10001
      keystore: classpath:user1/user1.pfx
      password: passworduser1
      minimal-replicas: 2

spring:
  datasource:
    url: jdbc:hsqldb:mem:user1;shutdown=true