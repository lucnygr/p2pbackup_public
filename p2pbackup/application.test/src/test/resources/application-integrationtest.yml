at:
  lucny:
    p2p-backup:
      user: user1
      network:
        port: 10001
      minimal-replicas: 2

spring:
  datasource:
    url: jdbc:hsqldb:mem:user1;shutdown=true

logging:
  level:
    org:
      hibernate:
        SQL: TRACE
        type: TRACE
